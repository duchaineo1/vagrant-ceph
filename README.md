### Vagrant ceph setup 

#### Prerequisites 

1. Vagrant
2. ansible

#### Setup

```bash
 git clone https://github.com/duchaineo1/vagrant-ceph.git
 cd vagrant-ceph
 vangrant up
 ansible-playbook playbook.yml -i inv.ini
 ``` 
```
